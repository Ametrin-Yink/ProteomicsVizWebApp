# Copy to frontend/.env.local for local development.
# Do not append /api; the API client adds it where required.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
# Production public gateway used when generating links from the private admin UI.
# NEXT_PUBLIC_REPORT_BASE_URL=http://10.202.25.39:8000
# Production Next.js server-side rewrite target:
# BACKEND_URL=http://127.0.0.1:8100
